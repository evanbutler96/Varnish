package com.example.database

import com.example.database.ui.theme.project.Project
/*
data class ProjectState(
    val projectId: Int = 0,
    val projectName: String = "",
    val projectStage: String = "",
    val projectFavorite: Int = 0,
    val actionEnabled: Boolean = false
)

fun ProjectState.toProject(): Project = Project(
    projectId = projectId,
    projectName = projectName,
    projectStage = projectStage,
    projectFavorite = projectFavorite
)

fun Project.toProjectState(actionEnabled: Boolean = false): ProjectState = ProjectState(
    projectId = projectId,
    projectName = projectName,
    projectStage = projectStage,
    projectFavorite = projectFavorite,
    actionEnabled = actionEnabled
)

fun ProjectState.isValid() : Boolean {
    return projectName.isNotBlank() && projectStage.isNotBlank()
}*/